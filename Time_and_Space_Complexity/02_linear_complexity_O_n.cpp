#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin >> n;

    // O(n) Time Complexity
    for (int i = 0; i < n; i++)
    {
        cout << i << " ";
    }

    return 0;
}